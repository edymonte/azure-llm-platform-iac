# ────────────────────────────────────────────────────────────────────────────────
# Terraform state and local configs (never commit sensitive data)
# ────────────────────────────────────────────────────────────────────────────────
**/.terraform/
**/*.tfstate
**/*.tfstate.*
**/*.tfplan
**/plan.tfplan
**/*.plan
**/full.plan
**/*.log
**/apply.log
**/plan.log

# Backend and variable files with real values
**/backend.tfvars
**/dev.tfvars

# OS / Editor
.DS_Store
Thumbs.db
.vscode/
*.swp

# Docs folder (local only)
docs/

# Local-only project memory (assistant retomada)
claude.md
